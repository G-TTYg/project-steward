interface:
  display_name: "Project Steward"
  short_description: "Keep project work stable, structured, documented, verified, and Git-clean."
  default_prompt: "Use $project-steward to preserve project structure, comments/headers, docs, Git hygiene, verification, and handoff clarity."
