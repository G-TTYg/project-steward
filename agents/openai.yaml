interface:
  display_name: "Project Steward"
  short_description: "Steer long Codex project work with checkpoints."
  default_prompt: "Use $project-steward to steward this project task with scoped context, checkpoints, verification, and a clean handoff."
